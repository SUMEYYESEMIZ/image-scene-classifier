---
title: image-scene-classifier
sdk: gradio
app_file: app.py
---

# Image Scene Classifier

This project performs image scene classification using a CNN (ResNet18).
The model is trained on a ready-made dataset and deployed with Gradio.

## How it works
- Upload dataset as zip
- Model trains automatically
- Upload an image to get prediction
